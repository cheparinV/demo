spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/demo
    username: postgres
    password: root
  jpa:
    hibernate.ddl-auto: create
    properties.hibernate.jdbc.lob.non_contextual_creation: true
storage.dir: uploader-test
